#include <bits/stdc++.h>
using namespace std;

class Solution {
private:
    bool solve(vector<int> &stones, int i, int sum) {
        if(i == )
    }
public:
    bool canCross(vector<int>& stones) {
        return solve(stones, 1, 0);
    }
};